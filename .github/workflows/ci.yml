name: Continuous Integration - FILCO

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Backend Dependencies
        run: |
          cd Revista_Back/backend
          npm install
      - name: Backend Lint
        run: |
          cd Revista_Back/backend
          # npx eslint . # Descomentar cuando esté configurado
          echo "Linting backend..."

  frontend-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Frontend Dependencies
        run: |
          cd Revista_Front/frontend
          npm install
      - name: Frontend Build
        run: |
          cd Revista_Front/frontend
          npm run build
      - name: Cypress Run (Headless)
        run: |
          echo "Placeholder: En un entorno Real, aquí se lanzaría Cypress"
          # npx cypress run
